# 2026-07-15 工作记录

## 项目扫描(纯探查,未写代码)
- 项目为双轨仓库: `poc/`(可运行 PoC) + 正式工程骨架(`apps/ services/ packages/ tests/`)。
- 隔离红线: 正式代码不得引用 `poc/`; 仅迁移已验证契约/状态机/测试场景。
- 合规驱动: 卫健委 CDSS 规范、电子病历分级访问、生成式 AI 办法; docs/requirements 下需求规格 v0.2 + 证据基线 v0.1(四级证据 A/B/C/D)。

## 关键发现
- `npm run contracts:test` 实测 0/2 失败: 正式侧 `tests/contracts/*.test.mjs` 与 `services/knowledge-orchestrator/src/ports.mjs` 均 import 根 `packages/clinical-contracts`,但该目录不存在; 契约实际只存在于 `poc/packages/clinical-contracts/`。文档声称 7/7 通过,与现状不符。
- `services/knowledge-orchestrator` 仅有端口边界骨架(10 个端口),无真实实现。
- `poc/` 能力: 病例状态机、服务端角色裁决、显式降级、混合检索(TF-IDF+多语言 MiniLM 384维)、知识生命周期、受控导入、JSON 运行时恢复。poc:test 文档声称 27/27(本次未实跑以避免下载嵌入模型)。

## 下一步候选(待用户确认)
1. 迁移/修正契约位置使 contracts:test 跑通并校准基线文档数字。
2. 确认正式工程启动顺序: packages/contracts → services 真实端口 → apps 前端。

## PoC 按需求文档打磨(用户指令: "把poc按需求文档跑完美一点")

### 契约迁移与扩展
- 将 `poc/packages/clinical-contracts/` 迁移到根 `packages/clinical-contracts/`，删除 poc 副本。
- 病例状态机扩展为 §3.4 完整契约: `confirmed/rejected/knowledge_changed/closed/cancelled` 全转移；`CASE_BLOCKING_STATES` 冻结。
- 知识版本状态机扩展为 §4.4: `superseded/archived/review_rejected`。
- `contracts:test` 修复为 7/7。

### workflow 对齐契约
- `workflow.mjs` 重写为 `import { assertCaseTransition }`，所有转移先校验。
- `reviewRisk()` 全部处理完自动 → `confirmed`(全确认)/`rejected`(有驳回)。
- 新增: `cancel()`→cancelled、`close()`→closed、`reconcile()`(reset+runAnalysis)、`onKnowledgeUnavailable()`(知识过期/撤回/被替代 → knowledge_changed 阻断)、`supplementTask()`(护士/个案管理师补充执行, §3.4)。
- `citation()` 增加 `coordinates` + `retrievedAt` (§4.3)。
- `server.mjs` knowledge transition 后加钩子触发阻断；新增端点 `/cancel /close /reconcile /tasks/:id/supplement`。

### 新增测试
- `poc/tests/workflow-state-machine.test.mjs` 9 例；`knowledge-lifecycle.test.mjs` 3 例。
- `poc:test` 从 27/27 扩展到 39/39。

### 前端同步(app.js / index.html / styles.css)
- `primaryAction()` 联动 confirmed/rejected→生成草稿、simulated_published→close、knowledge_changed→reconcile。
- `renderWorkflow()` 覆盖 confirmed/rejected/knowledge_changed/closed/cancelled 全分支。
- 新增护士任务补充对话框(supplement-dialog)与 `renderTasks` 补充按钮。
- `statusLabel`/`knowledgeStatusLabel`/`caseDot` 补全新状态。

### Bug 修复: resetRuntime 在受控环境失败
- 根因: WorkBuddy 沙箱 `genie-safe-delete.cjs` 拦截项目内 `rmSync`，导致 `POST /knowledge/runtime/reset` 返回 INTERNAL_ERROR。
- 修复: `resetRuntime` 的 `rmSync` 包 try/catch，失败时回退 `persistState()` 写回预置状态(等价语义)。
- 注意: 测试运行器用临时路径不受拦截，故原测试(断言文件删除)仍通过。

### 验证结果
- `poc:test` 39/39、`contracts:test` 7/7、新增端点 API 冒烟 12/12 全绿。
- 更新 `poc/docs/testing/05-poc-validation-results.md`(V-25~V-32)。
- 注意: 首次语义检索会下载 Xenova/paraphrase-multilingual-MiniLM-L12-v2 模型；核心工作流端点不需要模型。

## 需求↔PoC 双向对齐反馈(用户指令: "结合 docs/requirements 需求文档和另一个文档对齐反馈, 不急着写代码")

### 产出
- 新建 `docs/requirements/需求与PoC对齐反馈报告_v0.1.md`(差异矩阵+正向修订清单+反向标注清单)。

### 正向修订 PoC 文档(使其对齐需求 §3.4/§4.4)
- `poc/docs/architecture/03-状态机.md`: 补 knowledge_changed 阻断态及转移、close/cancel/reconcile/onKnowledgeUnavailable/supplementTask 动作行、知识 superseded/archived/review_rejected 终态、不变量第6条。
- `poc/docs/architecture/02-接口契约.md`: 补 6 端点(cancel/close/reconcile/supplement/knowledge-audit/demo-reset)、3 错误码(KNOWLEDGE_CHANGED/TASK_NOT_FOUND/RISK_NOT_FOUND)、transition 反向阻断副作用。
- `poc/docs/architecture/01-数据模型.md`: KnowledgeCitation 补 coordinates/retrieved_at；KnowledgeDocument.status 补完整枚举；TaskDraft.tasks 补 simulated_supplemented/execution_result；CaseState 补 knowledge_changed。
- `poc/docs/testing/04-e2e-cases.md`: 补 E2E-11~15。
- `poc/docs/CONTEXT.md`: 26/26→39/39、知识生命周期补终态、能力补第8条。
- `poc/docs/testing/05-poc-validation-results.md`: 补 E2E 映射段。

### 反向处理需求文档
- A 类(直接改正文): 需求§0表/§8.5、证据§3.1 的 27/27→39/39；§0.2 演示剧本补 close/reconcile/supplement 路径；§10.18 标注 PoC 已验证 closed/cancelled。
- B 类(加【PoC对齐反馈】批注,不改需求意图): §3.4 状态序列漏 knowledge_changed + 护士补充措辞含糊；§4.3 字段命名不统一；§4.4 uploaded/parsing 混入文档生命周期；§4.4 未定义 reconcile 恢复动作。

### 边界
- 本轮只改文档,不改代码;重跑 poc:test 39/39、contracts:test 7/7 确认未误改契约。
- B 类批注留待需求负责人决策,不越权改写需求意图。

## 反馈闭环关闭 + 浏览器验证 + 参考项目分析

### 方向一: 反馈闭环
- 5 条 B 类【PoC对齐反馈】批注全部融入需求正文并移除，需求文档零批注残留。
- §3.4: 状态序列补 knowledge_changed + reconcile 恢复 + 护士 supplementTask 明确覆盖两状态
- §4.3: 引用字段括注实现字段名(coordinates/retrieved_at/excerpt)
- §4.4: uploaded/parsing 拆为入库任务状态机/文档版本状态机两段；补 reconcile 恢复动作

### 方向四: 浏览器验证
- API 层 §0.2 6 步演示全部通过(close/reconcile/supplement 路径)
- 前端层 HTML/JS/CSS 加载验证通过(app.js 含 primaryAction/submitSupplement/close/reconcile)
- Playwright 因模块路径问题未跑截图，不影响结论

### 参考项目分析
- 探查 6 个参考开源项目: carehandoff-ai / chronicdiseasemanagement / medplum / hapi-fhir / ragflow / langgraph
- 产出 `docs/requirements/需求增强建议_来自参考项目_v0.1.md`: 18 条建议(P0×6/P1×8/P2×4)
- P0 可立即修订需求文档: 病历一致性四子任务、转归机构感知严重度、审核升级(escalate)、分块坐标保留、分块后处理管道、草稿幻觉守卫
- 最显著遗漏: 本需求完全没有患者/家属角色定义(§2 只有医护人员角色)
- 关键借鉴: CarePlan 作为出院+慢病协调资源、三层权限粒度、查询操作审计、On-Behalf-Of 服务代表模式、Agent 中断恢复检查点

## P0 增强项修订需求文档 + PM审查
- 产品经理许清楚独立审查 18 条建议，产出增量 PRD：6 条 P0 确认为必须纳入
- 将所有 P0 嵌入需求规格 v0.2 正文：
  - P0-1 §5: 病历一致性 Agent 拆四子任务(用药缺口/检验缺口/过敏冲突/剂量差异)
  - P0-2 §3.1: 出院去向感知的严重度动态调整(home/SNF/acute)
  - P0-3 §3.4: 审核增加 escalate 升级动作(可选配置,试点评估)
  - P0-4 §4.2: 表格/流程图/影像标注分块须保留结构化边界,不得展平为纯文本
  - P0-5 §4.2: 分块后处理管道(关键词/科室标签/有效期提取)
  - P0-6 §5.1: Agent 输出须标注证据溯源类型(source_ehr/knowledge/hybrid/none)
- PM 4 项待确认决策待用户拍板: 患者角色入§2 / escalate是否永久 / 字段级权限 / 紧急访问

## 决策拍板 + P1 增强 + AI智参扫描
- 4 项决策全部拍板:
  - 患者角色: §2 底部加"服务对象角色(后续阶段)"占位段,声明阶段 3 补
  - escalate: 维持当前状态(已纳入+标"可选配置")
  - 字段级权限: §2 末尾声明"阶段 1 详细设计时引入,参照 FHIR AccessPolicy 模式"
  - 紧急访问: 维持现状(§7+§10 已覆盖)
- P1 高优先级 5 条嵌入需求文档:
  - §6.1: 补充 PlanDefinition FHIR 资源 + CarePlan 双模式(出院/慢病两条并行)
  - §4.4: 知识版本回退(withdrawn→自动激活上一可用已发布版本)
  - §7: 查询操作审计(search/read 操作也生成 AuditEvent,记录查询 URL)
  - §6.2: 工作流检查点(人工确认节点前保存状态快照)
- AI智参平台实训教程 v3.2 深度扫描(16,554 段):
  - 产出 2 条 P0 补充: §4.2 混合检索权重和强制校验为 1.0、§8.4 检索降级链验收项
  - P1(中文分块分割符/健康检查模板/冒烟清单/会话隔离)存档正式工程参考
- 需求文档本轮累计增强: P0×6 + P1×5 + 决策×3 + 智参×2 = 16 条

## PoC 实现 P0 增强 (代码落地)
- 架构师高见远产出增量设计方案(5 任务分解+类图+时序图)
- 工程师寇豆码 T01-T05 全部实现:
  - workflow.mjs: 新增 dose_discrepancy 风险项(第4项)、全部 evidence 加 source_type、dischargeTo 严重度调整逻辑、escalate 升级动作
  - app.js: source-tag 溯源标签、escalate 升级按钮、escalated 状态禁用操作
  - server.mjs: reviewMatch 路由传 reason 参数
  - 测试: 5 个新用例(escalate/dischargeTo/source_type/data_conflict)
  - 文档: 03-状态机/04-e2e/05-验证/CONTEXT 同步更新
- QA 严过关独立验证: 44/44 全绿 + 5 项边界条件 API 冒烟全 PASS, 智能路由判定 NoOne
- 补齐 .source-tag 和 .escalate-button CSS 样式
- 测试从 39→44 通过

## §10 待确认项覆盖度扫描
- 产品经理许清楚逐项扫描 21 项: ⚠️部分覆盖 11 项(52%) / ❌纯医院决策 10 项(48%)
- 最有价值部分覆盖项: #18 任务状态机(PoC 已验证全转移)、#9 人工升级(escalate 技术路径通)、#5 知识库来源(生命周期 PoC 已验证)
- 需求寻找阶段闭环: 需求规整→PoC对齐→参考项目调研→P0+P1增强→PoC实现→§10扫描

## 项目初始化 + 层级优化
- git init (main 分支) + .editorconfig + .node-version(Node 22) + tsconfig.base.json + README.md
- .gitignore 补充: 参考借鉴项目/ / poc/data/ / *.mermaid
- 架构文档归位: docs/system_design.md→poc/docs/architecture/04-增量实现方案.md
- docs/ 层级净化: 只剩需求文档(docs/requirements/)+架构骨架(docs/architecture/)
- 初始提交 c597fc4: 58 files

## 架构总览 v0.1→v0.2 (技术选型决策升级)
- v0.1 (75d4b40): Node.js+Fastify+PostgreSQL(pgvector), 4服务架构
- v0.2 (e63a5b9): FastAPI(Python 3.12)+MySQL 8.0+Milvus Lite, 5 容器部署
- 前端: 四方案对比(MUI/Ant Design/Next.js/shadcn/ui), 推荐 MUI+Zustand+TanStack Query
- 后端重评估: Pydantic v2→Agent 校验、sentence-transformers 原生→嵌入推理、fhir.resources→FHIR 生态、Celery→异步管道、SQLAlchemy 2.0→ORM
- 数据库: MySQL(业务)+Milvus Lite(向量) 双库, 混合检索 Milvus+MySQL BM25→RRF

## 正式工程第一块基石: clinical-contracts Python移植 + workflow-engine骨架
- packages/clinical-contracts-py/: Pydantic v2 版契约(280行), 5 Enum+3转移表+角色权限
- services/workflow-engine/: FastAPI + SQLAlchemy + 3端点(create/analyse/review) + 58测试
- namespace package 设计: zhenhu.contracts / zhenhu.workflow 跨包共享
- 隔离红线零违规: grep -r "poc/" exit=1
- QA 严过关独立验证: 58/58 全绿 + API 冒烟 4 端点全 PASS + 红线零违规
- 提交 fed28fa

## 架构文档补齐 (coding硬门槛)
- 01-数据模型与存储方案: 4服务17表完整ER图+字段定义+MySQL三库隔离+Milvus+Redis
- 03-接口契约与API设计: 3服务23端点+11错误码+UnifiedResponse统一格式+跨服务Hook
- 现存代码(models.py 4表)与文档完全对齐,架构师以代码为基线扩展(零破坏)
- review_decisions 为唯一新增表(审核决策不可变记录)
- 提交 1fe19fb

## 今日总结
- Git: 5 commits (c597fc4→75d4b40→e63a5b9→fed28fa→1fe19fb)
- 测试: PoC 44/44 + contracts 7/7 + engine 58/58 = 109 项全绿
- 需求: v0.2 累计 16 条增强, 7/11 章节有实质性更新
- 文档: 架构 00/01/03 + 4张Mermaid图 + 对齐反馈报告 + 增强建议报告
- 技术栈冻结: FastAPI+MySQL+Milvus+Redis+MUI+Zustand+TanStack Query
- 下一步: knowledge-orchestrator 搭建(文档就位,可开写)
