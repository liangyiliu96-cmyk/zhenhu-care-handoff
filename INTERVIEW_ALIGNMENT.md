# 医疗 AI 项目考察对齐说明

本项目按考察要求定位为“医疗问答循证系统”，业务边界是出院交接、慢病随访和住院协同中的临床证据支持。它不是面向患者直接下诊断的聊天机器人，AI 输出只作为医护审核前的辅助建议。

## 与考察要求的对应关系

| 考察点 | 臻护当前实现 | 演示入口 |
| --- | --- | --- |
| 真实医疗场景 | 出院交接、护理任务、用药风险、慢病随访和病种路径 | 医生工作台、患者全貌、助手面板 |
| 问题理解与受控召回 | 助手意图识别；按角色、科室、病种、患者状态、知识层级和生效时间过滤 | 患者助手 |
| 证据检索与引用 | Milvus 本地 RAG + knowledge-orchestrator 受控检索；引用包含来源、版本、位置、来源类型、证据等级、指南年份和检索策略版本 | 助手回答中的证据来源 |
| 证据可核验 | Neo4j 证据图谱把证据、来源、知识层级、病种和临床规则关联起来；同病种方向性冲突会提示人工核验 | 管理端“证据图谱” |
| 版本治理 | review_pending → published → expired/withdrawn/superseded/archived 状态机；仅发布且当前有效版本可召回 | 管理端“知识库治理” |
| 失败降级 | 无证据、低相关、版本不一致、图谱不可用、索引异常分别返回诊断状态；不复用旧引用 | 助手降级提示与执行日志 |
| 可追溯审计 | 助手执行日志、知识生命周期事件、管理操作审计、图谱重建元数据 | 管理端治理页 |
| 个性化智能体加分项 | 患者状态、角色权限和病种上下文进入助手决策；规则优先、检索其次、模型最后 | 医生/护士不同工作台 |
| 完整应用闭环 | React 前端、FastAPI 服务、MySQL、Milvus、Neo4j、Redis、Keycloak、Docker Compose | `http://127.0.0.1:5173` |

## 核心循证链路

```text
用户问题
  -> 意图识别与角色边界
  -> 患者/病种/科室/知识版本上下文
  -> 分层检索与证据过滤
  -> 相关性、生命周期、图谱一致性校验
  -> 证据片段 + 来源 + 版本返回
  -> 模型生成可审核建议
  -> 助手执行日志与审计记录
```

## 面试演示建议

1. 用同一患者分别以医生和护士身份提问，展示召回范围和回答重点不同。
2. 先提问一个有明确病种证据的问题，再提问一个无关问题，展示引用不会沿用上一轮。
3. 在知识库中将一份文档撤回或发布新版本，打开证据图谱管理页，展示“需要重建”状态和最近变更文档。
4. 执行图谱重建，展示最近重建时间、证据来源数量和 Neo4j 关联路径。
5. 关闭或模拟知识服务不可用，展示助手保留可解释的降级原因，而不是显示看似正常的旧引用。

## 当前明确边界与下一阶段

- 当前 Neo4j 图谱是由受控知识源和病种模板构建的证据投影，不保存患者身份、患者状态或工作流决策。
- 当前知识编排服务的检索实现仍以 MySQL 全文匹配为主，Milvus 侧承担现有助手的向量召回；下一阶段可把统一向量重排和临床证据人工审核工作台正式纳入 knowledge-orchestrator。当前循证元数据中的可信度和等级是可解释的规则化提示，不能替代临床方法学评价。
- 当前交付部署形态是 Docker Compose，覆盖前端、4 个后端服务和 MySQL、Redis、Milvus、Neo4j、Keycloak 等依赖；本项目不把 K8s 作为当前演示前置条件。
- 项目是技术验证型 CDSS，演示数据为虚构数据，不能替代持证医护人员的临床判断。

## AI Native 开发记录要点

答辩时应如实说明：AI 用于需求拆解、跨服务代码实现、测试补充、故障定位和文档整理；人负责医疗边界、数据权限、引用正确性、失败处理、接口契约和最终验收。质量控制以类型契约、状态机、定向测试、全量测试、Lint、生产构建和可演示链路复核为准，不以模型生成代码本身作为质量证明。
