# 臻护临床完整度补全方案 v1.0

**日期**: 2026-07-16  
**目标**: 从"AI全自动流水线"升级为"医生监督+AI辅助"的真实住院系统  
**当前测试**: 86/86 (inpatient-ward)  
**预计测试**: 100+ (实施后)

---

## 一、当前状态回顾

### 已有（无需改动）
```
admission → medication_reconciliation → triage → monitoring → daily_round/lab_review/medication_adjust/transfer
    → discharge → handoff → doctor_review → patient_confirm
```

- 14 病种模板 / 86 关键指标
- 7 项评估量表 (NRS/NRS2002/Morse/Padua/MMSE/ADL/IADL)
- 39 药物规则 (31 相互作用 + 8 疾病禁忌)
- 9 个 LLM 节点 + DeepSeek V4
- 86 测试 / SKIP_BRIDGE 1.26s

### 缺失（本轮补齐）
1. **病史采集** — 无 CC、HPI、PMH、FH、SH、ROS
2. **体格检查** — 无 PE 叙事
3. **鉴别诊断** — 直接挂单病种，无 DDx
4. **护理记录** — 无给药记录、出入量、护理措施
5. **医生人工介入** — interrupt.py 永远 auto-accept，无真实确认卡点

---

## 二、目标全链路

```
POST /admissions
    → admission (load_template + FHIR + 过敏史 + 7项评估)
    → history_taking (NEW: CC + HPI + PMH + FH + SH + ROS)
    → physical_exam (NEW: PE 叙事 + 病种定制检查项)
    → ddx (NEW: LLM 鉴别诊断 TOP3-5)
    → medication_reconciliation
    → triage (risk_level + LLM风险摘要)
    → 🔒 doctor_admission_confirm (interrupt: 医生确认入院)
    → monitoring loop:
        → daily_round (SOAP LLM)
        → lab_review (LLM检验解读)
        → nursing (NEW: MAR + I/O + 护理措施)
        → medication_adjust (LLM调药)
            → 🔒 doctor_med_confirm (条件: has_adjustments)
        → transfer (LLM转科理由)
    → after_monitoring:
        → if discharge_decision == "approved":
            → 🔒 doctor_discharge_sign (interrupt: 医生签字)
            → discharge → handoff → doctor_review → patient_confirm
```

---

## 三、新增组件详细设计

### 3.1 node_history_taking — 病史采集

**文件**: `agent/nodes_clinical.py` (新建，放所有临床节点)

**遵循标准**: SOAP 框架 / OLDCARTS 七要素 / 中国病历书写规范

| 采集项 | 字段 | 类型 |
|--------|------|------|
| 主诉(CC) | `chief_complaint` — 患者原话 + 持续时间 | str |
| 现病史(HPI) | OLDCARTS: Onset(起病)/Location(部位)/Duration(持续)/Character(性质)/Aggravating(加重)/Relieving(缓解)/Timing(时间)/Severity(严重度) | dict |
| HPI叙事 | `hpi_narrative` — LLM生成的中文叙事段落 | str |
| 既往史(PMH) | 慢病史列表 / 手术史列表 / 住院史 / 输血史 | dict |
| 用药史(Meds) | `[{drug, dose, freq, route, adherence}]` | list[dict] |
| 家族史(FH) | 一级亲属疾病列表 (重点关注遗传/心血管/肿瘤) | dict |
| 社会史(SH) | 吸烟/饮酒/职业/居住环境/照护者 | dict |
| 系统回顾(ROS) | 按病种模板 `ros_focus_systems` 定制的检查清单 | dict |

**LLM 调用** (2处):
- `await provider.invoke("根据OLDCARTS数据生成中文HPI叙事段落...")` → hpi_narrative
- `await provider.invoke("根据病种生成ROS问题清单...")` → ros_questions

**数据来源**: 从 patient_fixtures 读取或 POST body 传入。无数据时输出空结构 + 标记 "insufficient_data"。

**普适性设计 — 14病种模板新增字段**:

```json
{
  "history_template": {
    "cc_required": true,
    "hpi_focus": ["胸痛性质", "放射部位", "发作诱因", "硝酸甘油效果"],
    "pmh_key": ["高血压", "糖尿病", "心梗史", "PCI/CABG史"],
    "fh_focus": ["早发冠心病(<55岁男/<65岁女)", "猝死家族史"],
    "sh_key": ["吸烟量(包年)", "饮酒情况"],
    "ros_focus_systems": ["心血管系统", "呼吸系统"]
  },
  "pe_template": {
    "required_systems": ["心血管", "呼吸"],
    "focus_items": ["颈静脉充盈(JVP)", "心界叩诊", "心脏听诊(S1/S2/杂音)", "双下肢水肿"]
  }
}
```

最多14×2 个扩展块，每个病种约10-20行 JSON。

---

### 3.2 node_physical_exam — 体格检查

**文件**: `agent/nodes_clinical.py`

**遵循标准**: Bates 体格检查指南 / 中国体格检查规范

| 系统 | 检查项 | 必检病种 |
|------|--------|----------|
| 一般情况 | 发育/营养/意识/体位 | 全部 |
| 生命体征 | T/HR/RR/BP/SpO2 (已有) | 全部 |
| HEENT | 头/眼/耳/鼻/喉 + 甲状腺触诊 | 甲亢 |
| 颈部 | JVP/气管/甲状腺/淋巴结 | 心衰(JVP) |
| 胸部-肺 | 视触叩听/呼吸音/啰音 | COPD/肺炎 |
| 胸部-心脏 | 心界/心音/杂音 | 心衰/冠心病 |
| 腹部 | 视触叩听/肝脾/压痛 | 肝硬化/消化道出血 |
| 四肢脊柱 | 水肿/畸形/活动度/DVT征 | 术后恢复 |
| 神经系统 | 颅神经/肌力/感觉/反射/病理征 | 卒中(NIHSS+GCS) |
| 皮肤 | 皮疹/压疮/出血点/黏膜炎 | 化疗后 |

**LLM 调用**: 
- `await provider.invoke("根据PE数据生成中文查体叙事段落...")` → pe_narrative

**普适性**: 每个病种通过 `pe_template.required_systems` + `pe_template.focus_items` 定制。未指定系统默认记录"未查"。

---

### 3.3 node_ddx — 鉴别诊断

**文件**: `agent/nodes_clinical.py`

**流程**:
```
主诉 + HPI关键发现 + PE关键发现 → LLM → TOP3-5 DDx (含概率+依据)
```

**LLM prompt 模板**:
```
根据以下入院数据生成TOP3-5鉴别诊断:
- 主诉: {chief_complaint}
- 现病史: {hpi_narrative}
- 体格检查关键发现: {pe_key_findings}
- 当前诊断: {current_disease_name}

返回JSON: {"ddx_list": [
  {
    "disease": "疾病名称",
    "probability": 0.0-1.0,
    "supporting": ["支持点1", "支持点2"],
    "against": ["不支持点1"],
    "next_step": "建议进一步检查"
  }
]}
```

**输出**: `ddx_list` (list[dict])

**注意**: DDx 不改变病种模板选择——仅作为医生决策参考。

---

### 3.4 node_nursing — 护理记录

**文件**: `agent/nodes_clinical.py`

| 子模块 | 内容 | 更新频率 |
|--------|------|----------|
| 生命体征 | 复用 monitoring 数据 | 每日/每班 |
| 给药记录(MAR) | `[{drug, dose, time, route, nurse}]` | 每次给药 |
| 出入量(I/O) | 入量(po+iv)、出量(urine+drain+other) | 每班汇总 |
| 护理措施 | 翻身/口腔/导管/伤口/压疮预防 | 每日 |
| 异常报告 | 疼痛加重、意识变化、管路异常 | 事件驱动 |

**输出**: `nursing_records` (list[dict]) + `nursing_alerts` (list[str])

**与 monitoring 的区别**: monitoring 判断"能不能出院"，nursing 记录"做了什么护理"。互补不冲突。

---

### 3.5 三处医生人工介入 (interrupt)

**文件**: `agent/interrupt.py` (扩展)

| 卡点 | 位置 | 触发 | 审核内容 |
|------|------|------|----------|
| 🔒1 入院确认 | triage → 之后 | 每次新入院 | 病史+查体+DDx+risk_level+评估 → accept/edit/dismiss |
| 🔒2 调药确认 | medication_adjust → 条件 | LLM建议调药时 | 调药方案+理由+紧急性 → accept/modify/dismiss |
| 🔒3 出院签字 | discharge_approved → 之后 | 监控达标 | 出院小结/用药/随访 → sign/hold |

**实现方式**:
```
request_doctor_review(context) → 
    {status: "pending", review_id: "..."}   # 需要医生确认
    → 医生通过API提交 {review_id, action: "accept"/"edit"/"dismiss"}
    → status变为 "reviewed"
```

**无前端时的回退** (开发模式):
- `DOCTOR_AUTO_APPROVE=true` 环境变量 → 自动 accept
- 默认 `DOCTOR_AUTO_APPROVE=true` (保持现有测试行为不变)
- 生产环境不设此变量 → 必须等待医生确认

---

## 四、InpatientState 新增字段

在 `agent/graph.py` 的 `InpatientState` TypedDict 中新增:

```python
# 病史 (history_taking node)
history_data: dict | None           # CC + HPI + PMH + FH + SH
hpi_narrative: str | None           # LLM 生成的 HPI 叙事
ros_findings: dict | None           # 系统回顾发现

# 体格检查 (physical_exam node)
pe_data: dict | None                # 各系统检查记录
pe_narrative: str | None            # LLM 生成的 PE 叙事

# 鉴别诊断 (ddx node)
ddx_list: list[dict] | None         # TOP N DDx

# 护理 (nursing node)
nursing_records: list[dict] | None  # MAR + I/O + 护理措施
nursing_alerts: list[str] | None    # 护理异常报告

# 医生介入 (interrupt)
interrupt_queue: list[dict] | None  # 待处理审核项
doctor_confirm_status: str | None   # pending/approved/rejected
```

---

## 五、病种模板扩展映射

每个 JSON 模板扩展两个字段。以 `hypertension.json` 为例:

```json
{
  "_comment": "高血压病种 — 病史+查体模板",
  "history_template": {
    "cc_required": true,
    "hpi_focus": ["头痛", "头晕", "视物模糊", "心悸", "鼻衄"],
    "pmh_key": ["高血压", "糖尿病", "CKD", "冠心病", "脑血管病"],
    "fh_focus": ["早发高血压家族史", "脑血管病家族史"],
    "sh_key": ["吸烟", "饮酒", "盐摄入量", "运动习惯"],
    "ros_focus_systems": ["心血管系统", "神经系统", "肾脏/泌尿系统"]
  },
  "pe_template": {
    "required_systems": ["心血管", "神经系统", "眼底"],
    "focus_items": [
      "双侧上肢血压", "颈动脉杂音", "心界叩诊", "心音及杂音",
      "周围血管征", "神经科查体", "眼底检查"
    ]
  }
}
```

**全部14模板补全**:

| 病种 | history_focus | pe_focus |
|------|--------------|----------|
| 高血压 | 头痛/头晕/CKD/心脑血管家族史 | 四测血压/颈动脉/心界/眼底 |
| 心衰 | 气促/水肿/端坐呼吸/出入量 | JVP/心界/肺底啰音/下肢水肿 |
| 糖尿病 | 多饮多尿/体重变化/低血糖事件/足部 | 足部检查/皮肤/神经感觉/眼底 |
| 冠心病 | 胸痛(OLDCARTS)/放射/硝酸甘油反应 | 心音/心尖搏动/肺部/外周脉搏 |
| 脑卒中 | 发病时间/瘫痪/失语/凝视/NIHSS | GCS/NIHSS/颅神经/肌力/病理征 |
| COPD | 咳嗽/咳痰/气促/吸烟史(包年) | 肺视诊/呼吸音/啰音/哮鸣音/紫绀 |
| 肺炎 | 发热/咳嗽/痰液性状/CURB65 | 肺听诊/叩诊浊音/呼吸频率/紫绀 |
| CKD | 水肿/尿量/泡沫尿/多囊肾家族史 | 血压/水肿/肾区叩痛/心包摩擦音 |
| AKI | 少尿/容量/肾毒性药物/造影剂 | 容量状态/血压/肺部/心电图 |
| 肝硬化 | 黄疸/腹围/呕血黑便/饮酒史 | 黄疸/肝掌/蜘蛛痣/腹水/肝脾肿大 |
| 消化道出血 | 黑便/呕血/腹痛/NSAID/抗凝 | 血压心率/腹部压痛/肠鸣音/黑便 |
| 甲亢 | 心悸/怕热/体重下降/眼征/情绪 | 甲状腺触诊/眼征/心率/手颤 |
| 术后恢复 | 手术日期/伤口疼/引流管/DVT预防 | 伤口/引流/肠鸣音/下肢DVT征 |
| 化疗后 | 化疗药/周期/恶心呕吐/口腔/发热 | 口腔黏膜/皮肤/中心静脉导管/淋巴结 |

---

## 六、Graph 编排变更

文件: `agent/graph.py`

### 6.1 新增节点注册

```python
from .nodes_clinical import (
    node_history_taking,
    node_physical_exam,
    node_ddx,
    node_nursing,
    node_doctor_confirm,
)

builder.add_node("history_taking", node_history_taking)
builder.add_node("physical_exam", node_physical_exam)
builder.add_node("ddx", node_ddx)
builder.add_node("nursing", node_nursing)
builder.add_node("doctor_confirm", node_doctor_confirm)
```

### 6.2 边变更

```python
# 原: admission → medication_reconciliation
# 新: admission → history_taking
builder.add_edge("admission", "history_taking")
builder.add_edge("history_taking", "physical_exam")
builder.add_edge("physical_exam", "ddx")
builder.add_edge("ddx", "medication_reconciliation")

# triage 之后插入 interrupt
builder.add_edge("triage", "doctor_confirm")

# monitoring → daily_round 之后增加 nursing
builder.add_edge("daily_round", "nursing")
builder.add_edge("nursing", "lab_review")

# doctor_confirm 条件边
builder.add_conditional_edges("doctor_confirm", after_doctor_confirm, {
    "monitoring": "monitoring",
    "discharge": "discharge",
})
```

### 6.3 after_monitoring 路由更新

```python
def after_monitoring(state) -> str:
    # 条件: discharge_decision=approved → doctor_confirm (而不是直接discharge)
    if state.get("discharge_decision") == "approved":
        return "doctor_confirm"   # 改为先让医生签字
    # 其余条件不变...
```

### 6.4 after_doctor_confirm 新路由

```python
def after_doctor_confirm(state) -> str:
    if state.get("doctor_confirm_status") == "approved":
        if state.get("discharge_decision") == "approved":
            return "discharge"    # 出院签字通过 → 走出院流程
        return "monitoring"       # 入院确认通过 → 开始监测
    return "monitoring"           # reject → 回监测循环
```

### 6.5 condition_edges 映射更新

```python
"doctor_confirm": {"monitoring": "monitoring", "discharge": "discharge"},
```

---

## 七、文件变更清单

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `agent/nodes_clinical.py` | 病史+PE+DDx+护理+doctor_confirm，5个新节点 |
| **修改** | `agent/graph.py` | 新增5节点+3边+END映射+InpatientState字段 |
| **修改** | `agent/interrupt.py` | 扩展为多状态 interrupt (pending/reviewed) |
| **修改** | `agent/__init__.py` | 导出 nodes_clinical |
| **修改** | `disease_templates/*.json` (14个) | 各增加 history_template + pe_template |
| **修改** | `routes/patient_fixtures.py` | 14患者增加 CC/HPI/PMH/FH/SH/PE数据 |
| **修改** | `agent/nodes_admission.py` | 已有 node_admission 不变，triage 新增 interrupt 标记 |
| **修改** | `agent/nodes_monitoring.py` | medication_adjust 调药→医生确认标记 |
| **修改** | `agent/nodes_handoff.py` | doctor_review 加 interrupt 入口 |
| **修改** | `agent/loop.py` | gen_input 补齐新字段 |
| **新建** | `tests/test_nodes_clinical.py` | 病史/PE/DDx/护理/confirm 单元测试 |

---

## 八、实施分批

### Batch A: 基础建设 (先做)
```
1. InpatientState + gen_input 新增字段
2. 14模板扩展 (history_template + pe_template)
3. node_history_taking 实现
4. node_physical_exam 实现
5. fixture 数据补全 (CC/HPI/PE)
→ 测试: 新增 ~15 个
```

### Batch B: 流程扩展
```
6. node_ddx 实现
7. node_nursing 实现
8. graph.py 编排更新 (新节点+新边+新路由)
→ 测试: 新增 ~5 个
```

### Batch C: 人工介入
```
9. interrupt.py 多状态扩展
10. node_doctor_confirm 实现
11. doctor_admission_confirm / doctor_med_confirm / doctor_discharge_sign 三个卡点
12. DOCTOR_AUTO_APPROVE 环境变量开发模式
→ 测试: 新增 ~5 个
```

### 验证跑通
```
13. 14患者全链路跑通 (SKIP_BRIDGE=true)
14. 86→100+ 测试全绿
15. 提交
```

---

## 九、启动验证命令

```bash
# 开发模式 (跳过桥接 + 自动医生确认)
SKIP_BRIDGE=true DOCTOR_AUTO_APPROVE=true bash start.sh

# 测试
SKIP_BRIDGE=true DOCTOR_AUTO_APPROVE=true python -m pytest tests/ -q

# 全链路跑通
SKIP_BRIDGE=true python -c "
import asyncio,os; os.environ['SKIP_BRIDGE']='true'; os.environ['DOCTOR_AUTO_APPROVE']='true'
# ... 14 patients full loop ...
"
```

---

## 十、风险与回退

| 风险 | 缓解 |
|------|------|
| graph.py 变更导致现有测试失败 | 每个 batch 提交后立即跑全量测试 |
| 14模板 JSON 格式错误 | 统一 validate 脚本检查 |
| interrupt 多状态复杂度过高 | C 期可降级为简单标记 (pending/approved)，Phase6 再完整实现 |
| 新节点函数太慢 (LLM调用多) | 每个 LLM 调用 try/except + 超时回退 |
| 不兼容现有 fixture 数据 | gen_input 新增字段全部设默认 None |

---

## 十一、完成后状态

| 维度 | 当前 | 目标 |
|------|------|------|
| Agent 节点 | 12 | **17** |
| LLM 调用点 | 9 | **13** (+HPI叙事/ROS/PE叙事/DDx) |
| 临床评估量表 | 7 | 7 (不变) |
| 药物规则 | 39 | 39 (不变) |
| 病种模板字段 | ~16 | ~20 (+history_template/pe_template) |
| 测试 | 86 | **100+** |
| 医生介入 | 0 | **3 卡点** |
| 全链路节点 | 9步 | **13步** |
