# 臻护 · 全局架构与文件层级手册

> 臻护 v2.0 | 2026-07-21 | 当前源码结构与运行架构 | 基于代码真相

> **当前校准**：源码文件数、组件行数和早期测试统计会随维护变化，不作为运行时事实。当前本地入口、OpenAPI 统计、三端路由和验证边界见 [臻护-代码现状基线.md](臻护-代码现状基线.md)。

---

## 目录

1. [项目顶层结构](#一项目顶层结构)
2. [六层技术架构](#二六层技术架构)
3. [后端完整文件层级](#三后端完整文件层级)
4. [前端完整文件层级](#四前端完整文件层级)
5. [路由挂载映射表](#五路由挂载映射表)
6. [Agent DAG 拓扑](#六agent-dag-拓扑)
7. [数据模型 ER 关系](#七数据模型-er-关系)
8. [服务依赖与端口](#八服务依赖与端口)
9. [前端组件树](#九前端组件树)
10. [数据流全景](#十数据流全景)
11. [测试架构](#十一测试架构)
12. [环境变量全表](#十二环境变量全表)

---

## 一、项目顶层结构

```
臻护出院交接与慢病智能协同管理平台/
│
├── apps/                              # 前端应用
│   └── frontend/                      # React SPA (Vite + TypeScript)
│       ├── src/                       # 124 源文件 (详细见 §四)
│       ├── public/
│       ├── vite.config.ts
│       ├── tsconfig.json
│       ├── package.json              # 18 dependencies
│       └── .env.development
│
├── services/                          # 后端微服务
│   ├── inpatient-ward/               # ★ 核心服务 — 住院协同引擎 (含 gateway + workflow)
│   │   ├── src/zhenhu/inpatient/     # 96 .py 源文件 (详细见 §三)
│   │   ├── tests/                    # 51 测试文件 (详细见 §十一)
│   │   ├── config/                   # 知识库 + 组织结构 JSON
│   │   │   ├── clinical_knowledge.json  # 385 条 RAG 知识
│   │   │   └── org_structure.json       # 医院人员配置
│   │   ├── pyproject.toml            # 14 dependencies
│   │   └── docker-compose.yml        # Milvus + Redis + Neo4j + FastAPI
│   │
│   ├── fhir-adapter/                 # FHIR 标准适配器 (外部集成)
│   ├── knowledge-orchestrator/       # 知识编排器 (独立微服务)
│   └── workflow-engine/              # 工作流引擎 (独立微服务)
│
├── packages/                         # 共享类型契约
│   ├── clinical-contracts            # TypeScript 契约包
│   └── clinical-contracts-py         # Python 契约包
│
├── docs/                             # 文档
│   ├── 臻护全流程项目开发文档/
│   │   ├── 臻护-项目需求与技术文档.md      # 1613 行 · 16 章
│   │   ├── 臻护-API速查手册.md             # 977 行 · 15 章
│   │   ├── 臻护-全局架构与文件层级手册.md    # ★ 本文档
│   │   └── RAG知识库维护手册.md            # 651 行 · 10 章
│   ├── architecture/                 # 设计文档 (早期)
│   └── requirements/                 # 需求文档 (早期)
│
├── scripts/                          # 运维与巡检脚本
├── tests/                            # 集成测试 (跨服务)
├── poc/                              # 概念验证 (冻结)
├── 参考借鉴项目/                      # 参考资料 (冻结)
│
├── README.md
├── start.sh
└── tsconfig.base.json
```

---

## 二、六层技术架构

```
┌──────────────────────────────────────────────────────────────┐
│  L1 · 表现层 (Presentation)                                    │
│  ──────────────────────────────────────────────────────────  │
│   React 18.3 · TypeScript 5.7 · Vite 6.3                      │
│   MUI v6 (CSS Var + OKLCH) · Recharts 2.15 · Lucide 0.400    │
│   TanStack Query v5 · Zustand v5 · React Hook Form + Zod      │
│   React Router v7 · Emotion 11                                │
│                                                               │
│   文件: apps/frontend/src/ (~120 .ts/.tsx)                      │
│   组件: 44 组件 (layout 4 + clinical 28 + admin 6 + shared 6)  │
│   路由: 7 页面                                               │
├──────────────────────────────────────────────────────────────┤
│  L2 · 网关层 (Gateway)                                          │
│  ──────────────────────────────────────────────────────────  │
│   FastAPI 0.109 · uvicorn · Pydantic v2                        │
│                                                               │
│   中间件链:                                                     │
│     CORS → Auth(header/jwt/oidc) → Idempotency → Route        │
│                                                               │
│   路由挂载: 多个 APIRouter → 当前 OpenAPI 164 路径 / 166 操作（含 /v1 兼容别名，详细见 §五） │
│   文件: routes/ (38 .py 模块)                                 │
├──────────────────────────────────────────────────────────────┤
│  L3 · 业务层 (Business Logic)                                  │
│  ──────────────────────────────────────────────────────────  │
│   Services (19 模块):                                          │
│     ClinicalFacade     · 事务一致性                            │
│     CareManagement     · 照护 CRUD                             │
│     ClinicalAlerts     · 告警路由                              │
│     ClinicalEvidence   · 证据链 + RAG 引用                     │
│     EvidenceGraph      · Neo4j 图谱                            │
│     FollowUpContacts   · 随访联系人                            │
│     ClinicalBrief      · 临床简报                              │
│     AgentFlow          · Agent 可视化                          │
│     PatientAccess      · 患者权限                              │
│     ManagementAccess   · 管理权限                              │
│     TransactionalState · 乐观锁                                │
│     RuntimeCache       · Redis/内存                            │
│     SchemaMigrations   · 数据库迁移                            │
│     Observability      · 日志+度量                             │
│     Idempotency        · 幂等去重                              │
│     PatientState       · 版本管理                              │
│     ApiContract        · 路由校验                              │
│     AssistantActionDrafts · AI 草稿                            │
│                                                               │
│   文件: services/ (19 .py)                                    │
├──────────────────────────────────────────────────────────────┤
│  L4 · Agent 层 (AI Engine)                                     │
│  ──────────────────────────────────────────────────────────  │
│   LangGraph DAG · 27 节点 · 3 工作流                           │
│   DeepSeek V4-Pro (LLM) · 385 条 RAG 知识                      │
│   评分引擎: NEWS2 / qSOFA / Padua / Morse / Braden             │
│   5 模式助手: doctor / nurse / pharmacist / patient / integrative │
│                                                               │
│   文件: agent/ (28 .py)                                       │
├──────────────────────────────────────────────────────────────┤
│  L5 · 数据层 (Data)                                            │
│  ──────────────────────────────────────────────────────────  │
│  ┌────────────┬──────────────┬──────────────┬──────────────┐  │
│  │ SQLite/    │ Milvus       │ Neo4j        │ Redis 7      │  │
│  │ MySQL 8.0  │ :19530       │ :7687        │ :6379        │  │
│  │            │              │              │              │  │
│  │ 17 ORM 模型 │ 16 Collections│ 证据图谱      │ 会话缓存     │  │
│  │ + State    │ 385 条       │ (Disease→    │ (降级内存)   │  │
│  │ Store 4表  │ 384-dim      │  Symptom→    │              │  │
│  │            │ IVF_FLAT     │  Evidence)   │              │  │
│  └────────────┴──────────────┴──────────────┴──────────────┘  │
│                                                               │
│   文件: models.py · routes/state_store.py · agent/rag_engine.py│
│        · agent/constants.py · services/evidence_graph.py      │
├──────────────────────────────────────────────────────────────┤
│  L6 · 基础设施层 (Infrastructure)                              │
│  ──────────────────────────────────────────────────────────  │
│   Docker · Nginx · Docker Compose                             │
│   DeepSeek API (外部) · FHIR Adapter (外部)                    │
│   OpenTelemetry (计划) · GitHub Actions CI/CD (计划)           │
└──────────────────────────────────────────────────────────────┘
```

---

## 三、后端完整文件层级

```
services/inpatient-ward/
│
├── docker-compose.yml            # Milvus + Redis + Neo4j + FastAPI
├── pyproject.toml                # 14 dependencies
├── Dockerfile
│
├── config/
│   ├── clinical_knowledge.json   # 385 条 RAG 知识 (17 sections)
│   ├── org_structure.json        # 医院人员配置
│   └── scoring_thresholds.yaml   # 评分阈值
│
├── src/zhenhu/inpatient/
│   │
│   ├── main.py                   # ★ FastAPI 入口: 中间件 + 路由挂载 + 生命周期
│   ├── models.py                 # ★ 17 ORM 数据模型
│   ├── schemas.py                # Pydantic 请求/响应 schema
│   ├── __init__.py
│   │
│   ├── middleware/                # ── 横切关注点 (3 文件) ──
│   │   ├── __init__.py
│   │   ├── auth.py              # header/jwt/oidc 三模式认证 + 角色路由保护
│   │   └── idempotency.py       # HTTP 幂等性中间件
│   │
│   ├── routes/                    # ── 路由层 (33 文件, 26 个 APIRouter) ──
│   │   ├── __init__.py
│   │   ├── route_schemas.py     # 共享 Pydantic schema
│   │   ├── state_store.py       # 状态存储后端 (SQLite/MySQL)
│   │   ├── patient_fixtures.py  # 测试夹具加载
│   │   │
│   │   ├── admin.py             # 认证/组织/种子/索引 (17 端点)
│   │   ├── admission.py         # 入院创建+查看 (2 端点)
│   │   ├── admission_clinical.py# 病史/PE/护理录入 (3 端点)
│   │   ├── alerts.py            # 告警确认+解决 (3 端点)
│   │   ├── assistant.py         # 助手对话+会话 (8 端点)
│   │   ├── assistant_action_drafts.py # 助手草稿 CRUD (6 端点)
│   │   ├── care_management.py   # 照护管理 CRUD (10 端点)
│   │   ├── cds_hooks.py         # CDS Hooks (6 端点)
│   │   ├── clinical_note.py     # 临床笔记 (1 端点)
│   │   ├── command.py           # 医生指令 (1 端点)
│   │   ├── dashboard.py         # Dashboard + Timeline (2 router)
│   │   ├── discharge.py         # 出院+交接 (2 端点)
│   │   ├── discharge_summary.py # 出院小结 (2 端点)
│   │   ├── evidence.py          # 证据链 (1 端点)
│   │   ├── lab_trends.py        # 检验趋势 (1 端点)
│   │   ├── monitoring.py        # 体征+检验录入 (2 端点)
│   │   ├── monitoring_overdue.py# 逾期监测 (1 端点)
│   │   ├── nurse_board.py       # 护理任务+KPI (5 端点)
│   │   ├── nursing.py           # 护理记录 (1 端点)
│   │   ├── patients.py          # 患者目录 (1 端点)
│   │   ├── pending_reviews.py   # 审核队列 (1 端点)
│   │   ├── query.py             # LLM 临床问询 (1 端点)
│   │   ├── review.py            # 审核提交 (1 端点)
│   │   ├── rounds.py            # 查房记录 (1 端点)
│   │   ├── scores.py            # 评分数据 (1 端点)
│   │   ├── vital_trends.py      # 体征趋势 (1 端点)
│   │   ├── ward_overview.py     # 病区看板 (10 端点)
│   │   ├── ward_priority.py     # 病区优先+待审 (4 端点)
│   │   │
│   │   ├── agent_flow.py        # ★ Agent 流程可视化 (1 端点)
│   │   ├── clinical_brief.py    # ★ 临床简报 (1 端点)
│   │   ├── evidence_graph.py    # ★ 证据图谱 Neo4j (2 端点)
│   │   ├── follow_up_contact.py # ★ 随访联系人 (2 端点)
│   │   └── rag_admin.py         # RAG 管理端 (4 端点)
│   │
│   ├── agent/                     # ── Agent 管线 (28 文件) ──
│   │   ├── __init__.py
│   │   ├── graph.py             # ★ LangGraph StateGraph DAG 构建 (23 节点)
│   │   ├── assistant.py         # ★ 5 模式助手引擎 (RAG + 流式 + 会话)
│   │   ├── rag_engine.py        # ★ Milvus 向量检索引擎 (16 层, 385 条)
│   │   ├── llm_utils.py         # DeepSeek V4-Pro 统一调用
│   │   ├── config.py            # Agent 全局配置
│   │   ├── constants.py         # DEPT_CHECKLIST (16 科室 67 项)
│   │   │
│   │   ├── nodes_admission.py   # 入院: admission + triage + med_recon (3 节点)
│   │   ├── nodes_clinical.py    # 临床: history + PE + DDx + nursing + shift (5 节点)
│   │   ├── nodes_monitoring.py  # 监测: monitoring + round + adjust + lab + transfer (5 节点)
│   │   ├── nodes_scoring.py     # 评分: NEWS2 + qSOFA + VTE + stroke + mdt + padua (6 节点)
│   │   ├── nodes_handoff.py     # 出院: discharge + handoff + review + confirm (4 节点)
│   │   ├── nodes_checkpoints.py # 检查点: doctor_confirm + med_confirm + discharge_sign (3 节点)
│   │   ├── nodes_batch.py       # 批量: batch_scoring (1 节点)
│   │   ├── nodes.py             # 节点路由入口
│   │   │
│   │   ├── assessments.py       # 入院评估 (疼痛/营养/跌倒/DVT)
│   │   ├── medication_rules.py  # 药物交互规则引擎
│   │   ├── prompts.py           # Prompt 模板 (5 助手 × N 场景)
│   │   ├── tools.py             # Agent 工具函数
│   │   ├── harness.py           # Agent 测试夹具
│   │   ├── loop.py              # Agent 循环控制
│   │   ├── interrupt.py         # Human-in-the-loop 中断
│   │   ├── audit.py             # Agent 审计
│   │   ├── metrics.py           # Agent 指标
│   │   ├── persistence.py       # Checkpoint 持久化
│   │   ├── outbox.py            # 发件箱事件
│   │   ├── fhir_sync.py         # FHIR 适配器同步
│   │   └── clinical_external.py # 外部临床系统集成
│   │
│   ├── services/                  # ── 业务服务层 (19 文件) ──
│   │   ├── __init__.py
│   │   ├── clinical_facade.py  # 临床工作流事务门面
│   │   ├── care_management.py  # 照护管理 CRUD
│   │   ├── clinical_alerts.py  # 告警规范化
│   │   ├── clinical_evidence.py# 证据链 + RAG 引用合并
│   │   ├── clinical_brief.py   # 临床简报构建
│   │   ├── evidence_graph.py   # Neo4j 证据图谱查询
│   │   ├── agent_flow.py       # Agent 流程状态
│   │   ├── assistant_action_drafts.py # AI 草稿管理
│   │   ├── follow_up_contacts.py # 随访联系人加密
│   │   ├── patient_access.py   # 患者级权限
│   │   ├── management_access.py# 管理操作权限
│   │   ├── transactional_state.py # 乐观锁状态同步
│   │   ├── patient_state.py    # 患者状态版本
│   │   ├── runtime_cache.py    # Redis/内存缓存
│   │   ├── schema_migrations.py# 数据库迁移
│   │   ├── observability.py    # 结构化日志 + HTTP 度量
│   │   ├── idempotency.py      # 幂等性保留
│   │   └── api_contract.py     # 路由唯一性校验
│   │
│   ├── hooks/                    # ── 外部系统桥接 (2 文件) ──
│   │   ├── __init__.py
│   │   └── zhenhu_bridge.py    # 臻护 Bridge API 客户端
│   │
│   ├── domain/                   # ── 领域模型 (占位) ──
│   │   └── __init__.py
│   │
│   └── disease_templates/        # ── 22 病种模板 JSON ──
│       ├── heart_failure.json    ├── hypertension.json
│       ├── cad.json              ├── atrial_fibrillation.json
│       ├── copd.json             ├── pneumonia.json
│       ├── asthma.json           ├── pe.json
│       ├── stroke.json           ├── delirium.json
│       ├── diabetes.json         ├── hyperthyroidism.json
│       ├── cirrhosis.json        ├── gi_bleeding.json
│       ├── pancreatitis.json     ├── aki.json
│       ├── ckd.json              ├── hip_fracture.json
│       ├── sepsis.json           ├── post_surgery.json
│       ├── severe_anemia.json    └── tumor_chemo.json
│
└── tests/                         # ── 48 测试文件 ──
    ├── conftest.py               # pytest 配置 + fixtures
    ├── request_helpers.py        # HTTP 测试工具
    │
    ├── test_agent_nodes.py       # Agent 节点单元测试
    ├── test_agent_harness.py     # Agent 测试夹具
    ├── test_agent_tools.py       # Agent 工具函数
    ├── test_agent_flow.py        # Agent 流程测试
    ├── test_medication_rules.py  # 药物规则引擎
    ├── test_assessments.py       # 评估测试
    │
    ├── test_care_management.py   # 照护管理
    ├── test_clinical_evidence.py # 临床证据
    ├── test_clinical_brief.py    # 临床简报
    ├── test_evidence_graph.py    # 证据图谱
    ├── test_follow_up_contacts.py# 随访联系人
    ├── test_follow_up_overview.py# 随访概览
    │
    ├── test_authentication.py    # 认证测试
    ├── test_api_contract.py      # API 契约
    ├── test_idempotency.py       # 幂等性
    ├── test_audit_trail.py       # 审计追踪
    │
    ├── test_assistant_modes.py   # 助手模式
    ├── test_assistant_context.py # 助手上下文
    ├── test_assistant_quick_questions.py
    ├── test_assistant_action_drafts.py
    │
    ├── test_dashboard_contract.py# Dashboard 契约
    ├── test_dashboard_medication_safety.py
    ├── test_dashboard_care_fixture.py
    │
    ├── test_nursing_task_completion.py
    ├── test_pending_review_contract.py
    ├── test_alert_lifecycle.py
    ├── test_discharge_pdf_export.py
    │
    ├── test_management_operations.py
    ├── test_organization_leadership.py
    ├── test_patient_access.py
    │
    ├── test_rag_reindex.py       # RAG 索引
    ├── test_rag_runtime_optimization.py
    │
    ├── test_state_version.py     # 状态版本
    ├── test_transactional_state.py
    ├── test_state_store_backup.py
    ├── test_state_merge.py
    ├── test_state_backend_safety.py
    ├── test_schema_migrations.py
    │
    ├── test_full_clinical_flow.py# 全临床流程 E2E
    ├── test_integration_e2e.py   # 集成测试
    ├── test_route_smoke.py       # 路由冒烟
    │
    ├── test_cds_hooks.py
    ├── test_deployment_config.py
    ├── test_mysql_integration.py
    ├── test_new_endpoints.py
    ├── test_observability.py
    └── test_outbox.py
```

---

## 四、前端完整文件层级

```
apps/frontend/
│
├── index.html                    # Vite HTML 入口
├── package.json                  # 18 dependencies + 5 devDependencies
├── vite.config.ts                # Vite 配置 (7 段 proxy)
├── tsconfig.json                 # TypeScript strict
├── .env.development              # VITE_AUTH_MODE=header
│
├── public/                       # 静态资源
│
└── src/                          # ★ 124 源文件
    │
    ├── main.tsx                  # ★ React 入口: QueryClient + BrowserRouter
    ├── App.tsx                   # ★ 7 路由 + lazy loading + Suspense
    ├── vite-env.d.ts            # Vite 类型声明
    │
    ├── config/                   # ── 配置 (1 文件) ──
    │   └── api.ts               # API_BASE, AUTH_MODE, OIDC_LOGIN_URL, ROUTES 常量
    │
    ├── core/                     # ── 基础设施 (20 文件) ──
    │   ├── api-client.ts         # ★ UnifiedResponse 解包 + 4xx/5xx/超时
    │   ├── auth-bridge.ts        # ★ 3 模式认证桥接 + URL 编码
    │   ├── default-route.ts      # 角色默认路由
    │   ├── require-auth.tsx      # 路由守卫组件
    │   ├── admin-tabs.ts         # 管理端标签页配置
    │   ├── assistant-modes.ts    # 助手模式元数据
    │   ├── doctor-workspace.ts   # 医生工作区配置
    │   ├── nurse-workspace.ts    # 护士工作区配置
    │   ├── login-mode.ts         # 登录模式工具
    │   ├── routes.ts             # 路由配置
    │   ├── runtime-events.ts     # 运行时事件
    │   │
    │   ├── api-client.test.ts
    │   ├── auth-bridge.test.ts
    │   ├── default-route.test.ts
    │   ├── admin-tabs.test.ts
    │   ├── doctor-workspace.test.ts
    │   ├── nurse-workspace.test.ts
    │   ├── login-mode.test.ts
    │   ├── routes.test.ts
    │   └── runtime-events.test.ts
    │
    ├── stores/                   # ── 状态管理 (1 文件) ──
    │   └── auth-store.ts         # Zustand: login + setIdentity + verifySession + logout
    │
    ├── services/                  # ── API 服务层 (14 + 7 测试) ──
    │   ├── patient-service.ts    # ★ 34 函数: Dashboard/Scores/Trends/Command/Care/Discharge/...
    │   ├── ward-service.ts       # ★ 12 函数: Pending/Patients/Alerts/Overview/Vitals/...
    │   ├── assistant-service.ts  # ★ 22 函数: Chat/Stream/Sessions/QuickQuestions/Drafts
    │   ├── admin-service.ts      # 18 函数: RAG/Org/Seed/Reindex/Stats/Capabilities
    │   ├── nurse-management-service.ts # 7 函数: Tasks/Priority/Checklist/KPI
    │   ├── alert-service.ts      # 4 函数: Acknowledge/Resolve
    │   ├── auth-service.ts       # 3 函数: Whoami/Login
    │   ├── education-service.ts  # 3 函数: Education Resources
    │   ├── evidence-service.ts   # 3 函数: Evidence/Graph
    │   ├── agent-flow-service.ts # 7 函数: Agent Flow State
    │   ├── clinical-brief-service.ts # 5 函数: Clinical Brief
    │   ├── follow-up-service.ts  # 1 函数: FollowUp Overview
    │   ├── patient-directory-service.ts # 1 函数: Patient Directory
    │   ├── review-service.ts     # 1 函数: Submit Review
    │   │
    │   ├── patient-service.test.ts
    │   ├── ward-service.test.ts
    │   ├── assistant-service.test.ts
    │   ├── admin-service.test.ts
    │   ├── nurse-management-service.test.ts
    │   ├── follow-up-service.test.ts
    │   └── patient-directory-service.test.ts
    │
    ├── hooks/                    # ── TanStack Query Hooks (7 文件) ──
    │   ├── use-admin.ts          # 11 导出: RAG/Org/Workload/Insights/CDS/EvidenceGraph
    │   ├── use-ward.ts           # 11 导出: Pending/Patients/Alerts/Overview/Vitals/Trends/...
    │   ├── use-nurse-management.ts # 7 导出: Tasks/Priority/Checklist/Shift/KPI
    │   ├── use-patient-dashboard.ts # 2 导出: Dashboard/EvidenceGraph
    │   ├── use-page-auth.tsx     # 3 导出: AuthUser/usePageAuth/useManagementPageAuth
    │   ├── use-follow-up.ts      # 1 导出: FollowUp Overview
    │   └── use-patient-directory.ts # 1 导出: Patient Directory
    │
    ├── components/               # ── React 组件 (47 + 8 测试) ──
    │   │
    │   ├── layout/              # ★ 布局壳 (5 文件)
    │   │   ├── AppShell.tsx      # 三段式壳: TopBar + LeftNav + main + FAB
    │   │   ├── TopBar.tsx        # Logo + 标题 + 返回/管理 + 用户 Chip
    │   │   ├── LeftNav.tsx       # 4 套角色菜单
    │   │   ├── GlobalAssistantLauncher.tsx # 助手浮动按钮
    │   │   └── GlobalAssistantLauncher.test.tsx
    │   │
    │   ├── admin/               # ★ 管理组件 (5 文件)
    │   │   ├── AdminDataPanels.tsx # 知识库热力图 + 条目搜索 + 层级筛选
    │   │   ├── EvidenceGraphPanel.tsx # Neo4j 证据图谱管理
    │   │   ├── NurseManagementPanel.tsx # 护理质控 KPI 交班核查
    │   │   ├── SystemOperationsPanel.tsx # 4 运维操作卡片
    │   │   └── DiseaseTemplatePanel.tsx # 22 病种模板管理
    │   │
    │   ├── clinical/            # ★ 临床组件 (25 + 8 测试)
    │   │   ├── PatientAssistantPanel.tsx # ★ 5 模式 ChatBox SSE 流式
    │   │   ├── DiffPanel.tsx     # ★ DDx/用药/出院 三模式审核
    │   │   ├── CareManagementPanel.tsx # 照护管理 CRUD
    │   │   ├── WardClinicalBoard.tsx # 病区临床看板
    │   │   ├── NursePatientDrawer.tsx # 护士患者抽屉
    │   │   ├── ClinicalMonitoringEntryPanel.tsx # 体征/检验录入
    │   │   ├── NurseWorkspacePanels.tsx # 护士工作区
    │   │   ├── NursingTaskCompletionDialog.tsx # 任务完成
    │   │   ├── NursePatientDirectoryPanel.tsx # 护士患者目录
    │   │   ├── DischargeEducationPanel.tsx # AI 患教
    │   │   ├── CommandBar.tsx    # 医生快捷指令
    │   │   ├── MedicationSafetyPanel.tsx # 药物交互
    │   │   ├── EvidenceGraphPathPanel.tsx # 证据路径可视化
    │   │   ├── EvidencePanel.tsx # RAG 引用卡片
    │   │   ├── FollowUpOverviewPanel.tsx # 随访概览
    │   │   ├── ClinicalBriefPanel.tsx # 临床简报
    │   │   ├── AgentFlowPanel.tsx # Agent 流程
    │   │   ├── NursingRecordsPanel.tsx # 护理记录
    │   │   ├── AlertLifecyclePanel.tsx # 告警管理
    │   │   ├── ClinicalIntakePanel.tsx # 入院采集
    │   │   ├── PatientClinicalQueryPanel.tsx # 临床问询
    │   │   ├── NursingEntryDialog.tsx # 护理录入
    │   │   ├── FollowUpContactPanel.tsx # 随访联系人
    │   │   ├── PatientDirectoryPanel.tsx # 患者目录
    │   │   ├── DischargeWorkflowPanel.tsx # 出院工作流
    │   │   └── RoundsManagementPanel.tsx # 查房管理
    │   │
    │   └── shared/              # ★ 共享组件 (4 文件)
    │       ├── Feedback.tsx      # LoadingSkeleton/ErrorBanner/EmptyState/Metric
    │       ├── WorkspaceHeader.tsx # 工作区标题栏
    │       ├── AppRuntimeGuards.tsx # 运行环境保护
    │       └── DepartmentLeadershipStrip.tsx # 科室领导信息条
    │
    ├── pages/                    # ── 页面 (7 文件) ──
    │   ├── HomePage.tsx          # /          品牌首页 + 特性展示 + 数据卡
    │   ├── LoginPage.tsx         # /login      3 模式认证 + 开发身份
    │   ├── WorkbenchPage.tsx     # /workbench  待审队列 + 查房顺序 + 病区患者
    │   ├── DashboardPage.tsx     # /patient/:id  9 面板 + 4 工作区
    │   ├── DischargePage.tsx     # /patient/:id/discharge  出院小结 + 患教 + 签字
    │   ├── NurseBoardPage.tsx    # /nurse      任务队列 + 监测 + 交班 + 核查
    │   └── AdminPage.tsx         # /admin      8 标签页 (知识/组织/病区/护理/...)
    │
    ├── types/                    # ── 类型定义 (8 文件) ──
    │   ├── admin.ts              # 管理端类型
    │   ├── api.ts               # API 通用类型
    │   ├── auth.ts              # 认证类型
    │   ├── evidence-graph.ts    # 证据图谱类型
    │   ├── follow-up.ts         # 随访类型
    │   ├── nurse-management.ts  # 护理管理类型
    │   ├── patient-dashboard.ts # Dashboard 类型
    │   └── ward.ts              # 病区类型
    │
    ├── utils/                    # ── 工具函数 (10 + 10 测试) ──
    │   ├── care-lifecycle-utils.ts
    │   ├── care-utils.ts
    │   ├── clinical-brief-utils.ts
    │   ├── command-utils.ts
    │   ├── dashboard-utils.ts
    │   ├── discharge-utils.ts
    │   ├── discharge-workflow.ts
    │   ├── discharge-pdf.ts     # PDF 导出
    │   ├── education-utils.ts
    │   ├── evidence-graph-utils.ts
    │   ├── nurse-patient-utils.ts
    │   ├── patient-detail-utils.ts
    │   └── round-display.ts
    │
    └── theme/                    # ── 主题 (2 文件) ──
        ├── tokens.ts             # OKLCH 色彩 Token
        └── app-theme.ts          # MUI Theme 配置
```

---

## 五、路由挂载映射表

### 5.1 前端路由

| 路径 | 页面 | 认证 | 作用 |
|------|------|:--:|------|
| `/` | HomePage | — | 品牌首页 + 受控入口 |
| `/login` | LoginPage | — | 3 模式认证 |
| `/workbench` | WorkbenchPage | doctor | 待审队列 + 告警 + 患者列表 |
| `/patient/:id` | DashboardPage | doctor | 9 面板 Dashboard |
| `/patient/:id/discharge` | DischargePage | doctor | 出院流程 + 患教 |
| `/nurse` | NurseBoardPage | nurse | 任务队列 + 监测 |
| `/admin` | AdminPage | adminOnly | 管理控制台 |
| `*` | → `/` | — | 404 通配 |

### 5.2 后端 APIRouter 挂载表

| Router 模块 | 挂载前缀 | 端点数 | 说明 |
|------|------|:--:|------|
| `admin.py` | `/inpatient` | 17 | 认证/组织/种子/索引/模板/统计/健康 |
| `admission.py` | `/inpatient` | 2 | 入院创建+查看 |
| `admission_clinical.py` | `/inpatient` | 3 | 病史/PE/护理录入 |
| `alerts.py` | `/inpatient` | 3 | 告警管理 |
| `assistant_action_drafts.py` | `/inpatient` | 6 | 助手草稿 |
| `care_management.py` | `/inpatient` | 10 | 照护管理 CRUD |
| `clinical_brief.py` | `/inpatient` | 1 | 临床简报 |
| `clinical_note.py` | `/inpatient` | 1 | 临床笔记 |
| `command.py` | `/inpatient` | 1 | 医生指令 |
| `dashboard.py` (router) | `/inpatient` | 1 | Dashboard |
| `dashboard.py` (timeline_router) | `/inpatient` | 1 | 事件时间线 |
| `discharge.py` | `/inpatient` | 2 | 出院+交接 |
| `discharge_summary.py` | `/inpatient` | 2 | 出院小结 |
| `evidence.py` | `/inpatient` | 1 | 证据链 |
| `evidence_graph.py` | `/inpatient` | 2 | 证据图谱 Neo4j |
| `follow_up_contact.py` | `/inpatient` | 2 | 随访联系人 |
| `agent_flow.py` | `/inpatient` | 1 | Agent 流程 |
| `lab_trends.py` | `/inpatient` | 1 | 检验趋势 |
| `monitoring.py` | `/inpatient` | 2 | 体征+检验录入 |
| `nursing.py` | `/inpatient` | 1 | 护理记录 |
| `query.py` | `/inpatient` | 1 | LLM 问询 |
| `review.py` | `/inpatient` | 1 | 审核提交 |
| `rounds.py` | `/inpatient` | 1 | 查房记录 |
| `scores.py` | `/inpatient` | 1 | 评分数据 |
| `vital_trends.py` | `/inpatient` | 1 | 体征趋势 |
| `rag_admin.py` | `/admin/rag` | 4 | RAG 管理端 |
| `nurse_board.py` | `/nurse` | 5 | 护理任务+KPI |
| `ward_overview.py` | `/ward` | 10 | 病区看板 |
| `ward_priority.py` | `/ward` | 4 | 病区优先+待审 |
| `monitoring_overdue.py` | `/monitoring` | 1 | 逾期监测 |
| `pending_reviews.py` | `/reviews` | 1 | 审核队列 |
| `patients.py` | `/patients` | 1 | 患者目录 |
| `assistant.py` | `/assistant` | 8 | 助手对话+会话 |
| `cds_hooks.py` | `/cds-services` | 6 | CDS Hooks |
| `main.py` (独立) | `/` | 3 | health/ready/metrics |
| **V1 别名** | `/v1/...` | 12 组 | 向后兼容 |

---

## 六、Agent DAG 拓扑

### 6.1 27 节点工作流图

```
入院流程 (Entry Flow) ─────────────────────────────────────┐
                                                            │
  admission ──→ history_taking ──→ physical_exam ──→ ddx    │
                  (LLM)              (LLM)          (LLM+回退)│
                                                            │
      └──→ medication_reconciliation ──→ triage            │
              (LLM+回退)              (条件LLM)              │
                                                            │
                └──→ doctor_confirm (状态机)                │
                        │                                   │
              ┌─────────┴──────────┐                        │
              ▼                    ▼                        │
        batch_scoring         stroke_antithrombotic        │
         ┌──┴──┬──┬──┐          (纯规则)                   │
         ▼     ▼  ▼  ▼            │                        │
      news2  qsofa padua vte      ▼                        │
    (规则+  (规则+ (纯  (纯    mdt_trigger                │
     LLM)   LLM) 规则)规则)   (纯规则)                     │
         │                              │                   │
         └──────────────┬───────────────┘                   │
                        ▼                                    │
              ┌─doctor_confirm 后──┐                        │
              ▼                    ▼                         │
         daily_round            handoff (直接出院)           │
         (LLM+回退)             (LLM+回退)                   │
              │                    │                         │
              ▼                    ▼                         │
          nursing              doctor_discharge_sign          │
         (条件LLM)              (状态机)                      │
              │                    │                         │
              ▼                    ▼                         │
        shift_summary          patient_confirm               │
        (LLM+回退)             (纯规则)                       │
              │                                               │
              ▼                                               │
          lab_review ──→ monitoring ──→ medication_adjust   │
          (纯LLM)        (纯规则)        (条件LLM)            │
                                              │               │
                                              ▼               │
                                          transfer            │
                                         (条件LLM)            │
                                              │               │
                                              ▼               │
                                       doctor_review          │
                                       (条件LLM)              │
                                              │               │
                                              ▼               │
                                         discharge ──→ ...   │
                                         (编排)              │
                                                            │
出院流程 (Discharge Flow) ──────────────────────────────────┘
  discharge (编排) → handoff (LLM+回退)
    → doctor_discharge_sign (状态机)
    → patient_confirm (纯规则)
```

### 6.2 节点分类统计

| 分类 | 数量 | 节点列表 |
|------|:--:|------|
| 纯 LLM | 3 | history_taking, physical_exam, lab_review |
| LLM + 规则回退 | 5 | ddx, daily_round, shift_summary, handoff, medication_reconciliation |
| 规则 + LLM 建议 | 5 | news2, qsofa, triage, transfer, doctor_review |
| 条件 LLM | 2 | nursing, medication_adjust |
| 纯规则 | 10 | monitoring, admission, vte_prophylaxis, stroke_antithrombotic, mdt_trigger, padua_score, discharge, patient_confirm, doctor_confirm, doctor_med_confirm, doctor_discharge_sign |
| 编排包装 | 1 | batch_scoring |

---

## 七、数据模型 ER 关系

### 7.1 17 模型 ER 关系总表

#### 7.1.1 核心临床域（5 表）

| # | 模型 | 物理表 | 主键 | 核心字段 | 外键 | 索引 | 数据量 | 增长 | 保留 |
|:--:|------|------|------|------|------|------|:--:|:--:|------|
| 1 | Patient | inpatient_patients | id CHAR(36) UUID | data_import_batch_id CHAR(36) NN · display_label VARCHAR(100) NN · basic_info JSON | — | ix_patient_data_batch · uq_patient_batch_label | 小(百-千) | 慢 | 永久 |
| 2 | InpatientRecord | inpatient_records | id CHAR(36) UUID | patient_id CHAR(36) NN · current_status VARCHAR(20) · current_phase VARCHAR(20) · risk_level VARCHAR(10) · bed_no VARCHAR(20) · admission_date DATE · expected_discharge_date DATE · actual_discharge_date DATE · admission_diagnosis JSON · chief_complaint TEXT | patient_id → inpatient_patients.id | ix_inpatient_records_patient_id | 中(千-万) | 每月数百 | 永久 |
| 3 | MedicalHistory | medical_histories | id CHAR(36) UUID | inpatient_record_id CHAR(36) NN · history_content JSON · submitted_by CHAR(36) · idempotency_key VARCHAR(100) · confirm_status VARCHAR(20) · confirmed_by CHAR(36) · confirmed_at DATETIME | inpatient_record_id → inpatient_records.id | ix_medical_histories_inpatient_record_id · uq_medical_history_inpatient_record · uq_medical_history_record_idempotency | 中(按住院 1:1) | 同住院 | 永久 |
| 4 | CurrentCondition | current_conditions | id CHAR(36) UUID | inpatient_record_id CHAR(36) NN · record_date DATE NN · condition_snapshot JSON · vital_signs JSON · confirmed_by CHAR(36) · confirmed_at DATETIME | inpatient_record_id → inpatient_records.id | ix_current_conditions_inpatient_record_id · uq_current_condition_record_date | 大(每日 1 条/患者) | 每日数十条 | 永久 |
| 5 | Attachment | attachments | id CHAR(36) UUID | inpatient_record_id CHAR(36) NN · medical_history_id CHAR(36) · file_name VARCHAR(255) · file_type VARCHAR(50) · file_path VARCHAR(512) · uploaded_by_actor_id CHAR(36) NN · source_type VARCHAR(64) NN · content_hash VARCHAR(64) NN · upload_idempotency_key VARCHAR(100) NN · byte_size INT NN · storage_key VARCHAR(512) NN · ocr_status VARCHAR(20) NN · ocr_engine VARCHAR(64) · ocr_result JSON · ai_extraction_result JSON | inpatient_record_id → inpatient_records.id · medical_history_id → medical_histories.id | ix_attachments_inpatient_record_id · ix_attachments_medical_history_id · ix_attachments_uploaded_by_actor_id · uq_attachment_patient_content_hash · uq_attachment_patient_upload_idempotency | 中(每住院 3-10 个) | 随入院 | 永久 |

#### 7.1.2 交接与出院域（5 表）

| # | 模型 | 物理表 | 主键 | 核心字段 | 外键 | 索引 | 数据量 | 增长 | 保留 |
|:--:|------|------|------|------|------|------|:--:|:--:|------|
| 6 | HandoffContext | handoff_contexts | id CHAR(36) UUID | inpatient_record_id CHAR(36) NN · discharge_instruction_id CHAR(36) NN · doctor_id CHAR(36) NN · handoff_version INT NN (DEFAULT 1) · handoff_content JSON · doctor_confirmed BOOL NN (DEFAULT 0) · confirmed_at DATETIME | inpatient_record_id → inpatient_records.id · discharge_instruction_id → discharge_instructions.id | ix_handoff_contexts_inpatient_record_id · ix_handoff_contexts_discharge_instruction_id · ix_handoff_contexts_doctor_id · uq_handoff_context_inpatient_record · uq_handoff_context_discharge_instruction | 小(按住院 1:1) | 同住院 | 永久 |
| 7 | PresentationItem | presentation_items | id CHAR(36) UUID | handoff_context_id CHAR(36) NN · projection_version INT NN (DEFAULT 1) · item_type VARCHAR(50) NN · item_content JSON · source_instruction_version INT | handoff_context_id → handoff_contexts.id | ix_presentation_items_handoff_context_id · uq_presentation_item_version_type | 中(每次交接 5-15 条) | 随出院 | 永久 |
| 8 | ItemFeedback | item_feedbacks | id CHAR(36) UUID | handoff_context_id CHAR(36) NN · presentation_item_id CHAR(36) NN · actor_id CHAR(36) NN · idempotency_key VARCHAR(100) NN · feedback_content JSON | handoff_context_id → handoff_contexts.id · presentation_item_id → presentation_items.id | ix_item_feedbacks_handoff_context_id · ix_item_feedbacks_presentation_item_id · ix_item_feedbacks_actor_id · uq_item_feedback_actor_idempotency | 大(每 item 1-3 条) | 随审核 | 永久 |
| 9 | DischargeInstruction | discharge_instructions | id CHAR(36) UUID | inpatient_record_id CHAR(36) NN · instruction_version INT NN (DEFAULT 1) · instruction_content JSON · is_current BOOL NN (DEFAULT 1) · confirm_status VARCHAR(20) · confirmed_by CHAR(36) · confirmed_at DATETIME | inpatient_record_id → inpatient_records.id | ix_discharge_instructions_inpatient_record_id · uq_discharge_instruction_version | 小(按住院 1:1, 多版本) | 同住院 | 永久 |
| 10 | ManualIntervention | manual_interventions | id CHAR(36) UUID | handoff_context_id CHAR(36) NN · related_feedback_id CHAR(36) · presentation_item_id CHAR(36) · actor_id CHAR(36) NN · idempotency_key VARCHAR(100) NN · intervention_content JSON | handoff_context_id → handoff_contexts.id · related_feedback_id → item_feedbacks.id · presentation_item_id → presentation_items.id | ix_manual_interventions_handoff_context_id · ix_manual_interventions_actor_id · uq_manual_intervention_actor_idempotency | 小(按需) | 慢 | 永久 |

#### 7.1.3 体征与随访域（2 表）

| # | 模型 | 物理表 | 主键 | 核心字段 | 外键 | 索引 | 数据量 | 增长 | 保留 |
|:--:|------|------|------|------|------|------|:--:|:--:|------|
| 11 | BPEntry | vital_sign_entries | id CHAR(36) UUID | handoff_context_id CHAR(36) NN · presentation_item_id CHAR(36) · input_actor_id CHAR(36) · related_feedback_id CHAR(36) · idempotency_key VARCHAR(100) NN · systolic_mmhg INT · diastolic_mmhg INT · measured_at DATETIME · recorded_at DATETIME NN (DEFAULT now) | handoff_context_id → handoff_contexts.id · presentation_item_id → presentation_items.id · related_feedback_id → item_feedbacks.id | ix_vital_sign_entries_handoff_context_id · ix_vital_sign_entries_presentation_item_id · uq_vital_sign_context_idempotency | 大(每患者每天 3-6 条) | 每日数十条 | 3 年→归档 |
| 12 | FollowUpContact | follow_up_contacts | patient_id VARCHAR(128) PK | encrypted_payload TEXT NN · consented BOOL NN (DEFAULT 0) · preferred_channel VARCHAR(20) · contact_version INT NN (DEFAULT 1) | — (独立, 加密存储) | — | 小(按患者 1:1) | 慢 | 永久 (加密) |

#### 7.1.4 系统与审计域（3 表）

| # | 模型 | 物理表 | 主键 | 核心字段 | 外键 | 索引 | 数据量 | 增长 | 保留 |
|:--:|------|------|------|------|------|------|:--:|:--:|------|
| 13 | AuditLog | audit_logs | id CHAR(36) UUID | actor_id CHAR(36) · actor_role ENUM(patient/family/caregiver/doctor/nurse/coordinator/supervisor/system) NN · action_type VARCHAR(100) NN · target_table VARCHAR(100) · target_record_id CHAR(36) · action_detail JSON · session_id VARCHAR(100) | — (不级联删除) | ix_audit_logs_actor_id · ix_audit_logs_created_at · ck_audit_log_actor_role | 大(每次写操作 1 条) | 每日数百条 | 5 年→归档 |
| 14 | OutboxEvent | outbox_events | id CHAR(36) UUID | event_type VARCHAR(100) NN · payload JSON NN · idempotency_key VARCHAR(160) NN · status VARCHAR(20) NN (DEFAULT pending) · attempts INT NN (DEFAULT 0) · next_attempt_at DATETIME · delivered_at DATETIME · last_error TEXT | — | uq_outbox_event_idempotency · ix_outbox_event_delivery | 小(正在投递的) | 按事件 | delivery 后 7 天 |
| 15 | IdempotencyRecord | idempotency_records | id CHAR(36) UUID | scope VARCHAR(512) NN · idempotency_key VARCHAR(100) NN · request_fingerprint VARCHAR(64) NN · status VARCHAR(20) NN (DEFAULT processing) · response_status INT · response_body JSON | — | uq_idempotency_scope_key · ix_idempotency_created_at | 小(在有效期的) | 按 API 调用 | 24h TTL |

#### 7.1.5 工作流状态域（2 表）

| # | 模型 | 物理表 | 主键 | 核心字段 | 外键 | 索引 | 数据量 | 增长 | 保留 |
|:--:|------|------|------|------|------|------|:--:|:--:|------|
| 16 | ClinicalWorkflowState | clinical_workflow_states | patient_id VARCHAR(128) PK | state_json JSON NN · state_version INT NN | — | — | 小(按活跃患者 1:1) | 按入院 | 出院后 30 天 |
| 17 | TimestampMixin | (所有表 Mixin) | — | created_at DATETIME NN (DEFAULT now) · updated_at DATETIME NN (ON UPDATE now) | — | — | — | — | — |

### 7.2 关系拓扑图

```
                      ┌────────────────────┐
                      │  inpatient_patients │ 患者主档
                      │  ─────────────────  │
                      │  PK id              │
                      │  name, mrn, dob     │
                      │  gender, contact    │
                      └────────┬───────────┘
                               │ 1:N
                               ▼
                      ┌────────────────────┐
                      │  inpatient_records  │ 住院记录
                      │  ─────────────────  │
                      │  PK id              │
                      │  FK patient_id      │
                      │  admission_no       │
                      │  admission_date     │
                      │  discharge_date     │
                      │  department         │
                      │  attending_doctor   │
                      │  bed_number         │
                      │  status             │
                      └──┬──┬──┬──┬──┬─────┘
          ┌──────────────┘  │  │  │  │
          ▼                 │  │  │  │
┌──────────────────┐        │  │  │  │
│medical_histories │        │  │  │  │
│──────────────────│        │  │  │  │
│FK record_id      │        │  │  │  │
│chronic_diseases  │        │  │  │  │
│surgeries         │        │  │  │  │
│allergies         │        │  │  │  │
│medications       │        │  │  │  │
│family_history    │        │  │  │  │
└──────────────────┘        │  │  │  │
                            │  │  │  │
┌──────────────────┐        │  │  │  │
│current_conditions│◄───────┘  │  │  │
│──────────────────│           │  │  │
│FK record_id      │           │  │  │
│chief_complaint   │           │  │  │
│present_illness   │           │  │  │
│physical_exam     │           │  │  │
│current_medications│          │  │  │
└──────────────────┘           │  │  │
                               │  │  │
┌──────────────────┐           │  │  │
│discharge_        │◄──────────┘  │  │
│instructions      │              │  │
│──────────────────│              │  │
│FK record_id      │              │  │
│medications       │              │  │
│diet, activity    │              │  │
│follow_up         │              │  │
│emergency_signs   │              │  │
└──────────────────┘              │  │
                                  │  │
┌──────────────────┐              │  │
│clinical_workflow_│◄─────────────┘  │
│states            │                 │
│──────────────────│                 │
│FK record_id      │                 │
│workflow_type     │                 │
│state, version    │                 │
└──────────────────┘                 │
                                     │
┌──────────────────┐                 │
│vital_sign_entries│◄────────────────┘
│──────────────────│
│FK record_id      │
│systolic/diastolic│
│heart_rate, spo2  │
│temperature       │
│recorded_at       │
└──────────────────┘

┌──────────────────┐      ┌──────────────────┐
│handoff_contexts  │      │follow_up_contacts │
│──────────────────│      │──────────────────│
│FK record_id      │      │FK record_id       │
│version, status   │      │name, relationship │
│created_by        │      │phone(加密)        │
│acknowledged_by   │      │email              │
└────────┬─────────┘      │preferred_method   │
         │ 1:N             └──────────────────┘
         ▼
┌──────────────────┐
│presentation_items│
│──────────────────│
│FK handoff_id     │
│category, content │
│order             │
└────────┬─────────┘
         │ 1:N
         ▼
┌──────────────────┐      ┌──────────────────┐
│item_feedbacks    │      │manual_intervent  │
│──────────────────│      │──────────────────│
│FK item_id        │      │FK record_id       │
│reviewer, decision│      │intervention_type  │
│comment           │      │reason, operator   │
└──────────────────┘      └──────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│audit_logs        │  │outbox_events     │  │idempotency_      │
│──────────────────│  │──────────────────│  │records           │
│FK patient_id     │  │event_type        │  │──────────────────│
│action, actor     │  │payload           │  │idempotency_key   │
│details (JSON)    │  │status            │  │response          │
│timestamp         │  │created_at        │  │expires_at        │
└──────────────────┘  └──────────────────┘  └──────────────────┘

┌──────────────────┐
│attachments       │
│──────────────────│
│FK record_id      │
│file_name, type   │
│file_path         │
│uploaded_by       │
└──────────────────┘
```

### 7.3 State Store (4 表)

```
┌────────────────────┐  ┌────────────────────┐
│ patient_states     │  │ org_staff          │
│──────────────────  │  │──────────────────  │
│ patient_id (PK)     │  │ actor_id (PK)      │
│ state (JSON)       │  │ name, role         │
│ version            │  │ title, department  │
│ updated_at         │  │ is_active          │
│ ttl                │  └────────────────────┘
└────────────────────┘
┌────────────────────┐  ┌────────────────────┐
│ disease_templates  │  │ dept_checklists    │
│──────────────────  │  │──────────────────  │
│ disease_id (PK)    │  │ department (PK)    │
│ template (JSON)    │  │ checklist (JSON)   │
│ version            │  │ updated_at         │
└────────────────────┘  └────────────────────┘
```

### 7.4 外部数据库

```
┌─────────────────────────────────┐
│ Milvus :19530                   │
│ ──────────────────────────────  │
│ 16 Collections                  │
│ 385 条 · 384-dim · IVF_FLAT    │
│ 查询缓存: OrderedDict 512 条   │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ Neo4j :7687                     │
│ ──────────────────────────────  │
│ Nodes: Disease, Symptom,        │
│   LabFinding, Medication,       │
│   Guideline, Evidence, Patient  │
│ Relations: CAUSES, TREATED_BY,  │
│   INDICATES, SUPPORTS,          │
│   CONTRAINDICATES, INTERACTS    │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ Redis 7 :6379                   │
│ ──────────────────────────────  │
│ 助手会话 (24h TTL)              │
│ Key: asst:{session_id}          │
│ 降级: 内存 OrderedDict          │
└─────────────────────────────────┘
```

---

## 八、服务依赖与端口

```
                         ┌──────────────────┐
                         │   用户浏览器       │
│   127.0.0.1:5173   │
                         └────────┬─────────┘
                                  │ Vite Proxy (7 段)
                                  │ /inpatient/* /ward/* /nurse/*
                                  │ /assistant/* /admin/* /cds-services/* /patients
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│              inpatient-ward :8001                            │
│               (container :8000)                              │
│                   uvicorn --reload                            │
│                   内部端口, Nginx 反向代理                     │
└────┬─────────┬──────────┬──────────┬──────────┬─────────────┘
     │         │          │          │          │
     ▼         ▼          ▼          ▼          ▼
┌─────────┐ ┌──────┐ ┌──────────┐ ┌──────┐ ┌──────────────┐
│SQLite / │ │Milvus│ │ Neo4j    │ │Redis │ │ DeepSeek API │
│MySQL    │ │      │ │          │ │      │ │              │
│ 8.0     │ │2.x   │ │ 5.x      │ │ 7    │ │ V4-Pro       │
│ :3306   │ │:19530│ │ :7687    │ │:6379 │ │ (外部 HTTPS) │
│         │ │      │ │          │ │      │ │              │
│ 17+4    │ │16    │ │ 证据图谱  │ │会话   │ │ LLM 推理     │
│ 表      │ │集合   │ │          │ │缓存   │ │              │
│         │ │      │ │          │ │      │ │              │
│开发 SQLite│ │必需  │ │ 可选     │ │可选   │ │按节点可降级 │
│生产 MySQL │ │      │ │          │ │      │ │              │
└─────────┘ └──────┘ └──────────┘ └──────┘ └──────────────┘
```

| 服务 | 端口 | 状态 | 降级策略 |
|------|:--:|:--:|------|
| FastAPI | 8000 | 必需 | — |
| Vite Dev Server | 5173 | 必需 (dev) | 构建静态文件 (prod) |
| MySQL | 3306 | 必需 (prod) | SQLite (dev) |
| Milvus | 19530 | 必需 (RAG) | MiniLM CPU 直接检索 (无降级) |
| Redis | 6379 | 可选 | 内存 OrderedDict 会话 |
| Neo4j | 7687 | 可选 | 证据图谱功能不可用 |
| DeepSeek API | HTTPS | 必需 (LLM) | 回退规则 (Agent 节点) |

---

## 九、前端组件树

```
App
├── HomePage (/)
│   ├── Header (Logo + Chip)
│   ├── Hero (Title + CTA + HealthAssistant FAB)
│   ├── Features (4 卡片)
│   ├── Stats (3 数据卡)
│   └── PatientAssistantPanel (publicAccess)
│
├── LoginPage (/login)
│   ├── BrandLogo + ModeChip
│   ├── AuthCard
│   │   ├── AlreadyLoggedInAlert (已登录提示)
│   │   ├── CredentialsForm (jwt 模式)
│   │   └── OIDCButton (oidc 模式)
│   └── DevIdentitySection (header 模式)
│       └── 5 开发身份卡片
│
├── WorkbenchPage (/workbench)
│   └── AppShell
│       ├── TopBar ("医生工作台")
│       ├── LeftNav (医生菜单)
│       ├── [主内容]
│       │   ├── AlertLifecyclePanel / ShiftSummary (告警 + 交接摘要)
│       │   ├── PendingQueue (4 标签: DDx/用药/出院/告警)
│       │   │   └── DiffPanel (侧滑审核面板 — 整合入院/用药/出院三种审核模式)
│       │   │       └── EvidencePanel (RAG 引用来源)
│       │   ├── WardPatients (患者列表)
│       │   ├── VisitOrder (查房排序)
│       │   └── WardClinicalBoard (病区临床看板)
│       └── [右栏] AI Summary / AdmissionLauncher
│
├── DashboardPage (/patient/:id)
│   └── AppShell
│       ├── TopBar ("Dashboard · 患者名")
│       ├── LeftNav
│       ├── CommandBar (转科/会诊/出院/暂停)
│       ├── [概览工作区]
│       │   ├── HeaderRow (病种badge · NEWS2色标 · 天数)
│       │   ├── PanelA: ClinicalIntakePanel
│       │   ├── PanelB: ScoresPanel (NEWS2/qSOFA/Padua/Morse/Braden)
│       │   ├── PanelC: MedicationSafetyPanel
│       │   ├── PanelD: VitalsPanel (体征折线图)
│       │   ├── PanelE: LabTrendsPanel (检验趋势)
│       │   ├── PanelF: RoundsPanel (SOAP 四列)
│       │   ├── PanelG: CareManagementPanel (照护)
│       │   ├── PanelH: EvidenceGraphPathPanel (证据路径)
│       │   └── PanelI: AlertLifecyclePanel (告警)
│       ├── [查房工作区]
│       │   └── RoundsManagementPanel
│       ├── [医嘱工作区]
│       │   ├── CareManagementPanel
│       │   ├── ClinicalMonitoringEntryPanel
│       │   └── NursingRecordsPanel
│       ├── [记录工作区]
│       │   ├── FollowUpOverviewPanel
│       │   ├── ClinicalBriefPanel
│       │   ├── AgentFlowPanel
│       │   └── EvidencePanel
│       └── PatientAssistantPanel (医生/药师/中西医 3 模式)
│
├── DischargePage (/patient/:id/discharge)
│   └── AppShell
│       ├── TopBar ("出院小结 · 患者名")
│       ├── SummaryPanel (诊断/用药/随访 3 标签)
│       ├── PrintableDischargeSummary (PDF 导出)
│       ├── DischargeActions (签字/拒签)
│       ├── HandoffCompletionPanel (4 步闭环)
│       ├── DischargeEducationPanel (AI 患教)
│       ├── FollowUpContactPanel (随访联系人)
│       ├── MedicationSafetyPanel (用药安全)
│       └── PatientAssistantPanel
│
├── NurseBoardPage (/nurse)
│   └── AppShell
│       ├── TopBar ("护理看板")
│       ├── LeftNav (护士菜单)
│       ├── [概览] NurseShiftOverview (AI 优先级 + 指标卡)
│       ├── [任务] NursingPatientTasks + NursingTaskCompletionDialog
│       ├── [患者] NursePatientDirectoryPanel
│       ├── [逾期] MonitoringOverduePanel
│       ├── [交班] ShiftSnapshot + NurseManagementPanel(handoff)
│       └── [核查] DepartmentChecklist
│
└── AdminPage (/admin)
    └── AppShell
        ├── TopBar ("管理控制台")
        ├── LeftNav (管理菜单)
        ├── [知识库] AdminDataPanels → KnowledgePanel (热力图 + 搜索)
        ├── [组织] AdminDataPanels → OrganizationPanel
        ├── [病区] AdminDataPanels → WardPanel (工作量 + 洞察)
        ├── [护理] NurseManagementPanel(nursing, showKpi)
        ├── [交班] NurseManagementPanel(handoff)
        ├── [核查] NurseManagementPanel(checklist)
        ├── [图谱] EvidenceGraphPanel
        ├── [集成] AdminDataPanels → CDS Integration
        └── [运维] SystemOperationsPanel (4 操作卡片)
```

---

## 十、数据流全景

```
┌──────────────────────────────────────────────────────────────┐
│                      请求数据流                                │
│                                                              │
│  用户操作 → Component → useQuery/useMutation (TanStack)      │
│                │              │                              │
│                │   缓存命中    │ 缓存未命中/写操作               │
│                ▼              ▼                              │
│          React State     Service API 调用                     │
│                           │                                  │
│                           ▼                                  │
│                     api-client.ts                             │
│                     ├─ auth-bridge 注入 headers               │
│                     ├─ UnifiedResponse 解包                   │
│                     ├─ 4xx/5xx 错误 → ApiClientError          │
│                     └─ AbortController 超时                   │
│                           │                                  │
│                           ▼                                  │
│                Vite Proxy → inpatient-ward :8001              │
│                           │                                  │
│              ┌────────────┼────────────┐                     │
│              ▼            ▼            ▼                     │
│          middleware    routes/      agent/                    │
│          auth          .py          assistant.py              │
│          idempotency   ──→ 业务层 ←── graph.py (DAG)         │
│                        services/   llm_utils.py              │
│                           │            │                     │
│              ┌────────────┼────────────┤                     │
│              ▼            ▼            ▼                     │
│          MySQL        Milvus       DeepSeek API               │
│          SQLite       Neo4j        (LLM 推理)                 │
│          Redis                                                  │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                      Agent 数据流                              │
│                                                              │
│  入院触发: POST /admissions                                   │
│    → node_admission → DAG 串联 → 各节点读写 state             │
│    → 节点间 state 传递: dict → 序列化到 State Store           │
│    → Human-in-the-loop: doctor_confirm 暂停 → 等待 POST /review │
│                                                              │
│  查房触发: GET /dashboard + POST /command                     │
│    → 读取 State Store 最新 state                             │
│    → node_daily_round → LLM 增强 SOAP                        │
│    → node_medication_adjust → LLM 调药建议                    │
│                                                              │
│  助手触发: POST /assistant/chat/stream                        │
│    → _expand_query (同义词改写)                               │
│    → Milvus 向量检索 → _rerank_hits → top 3                  │
│    → 注入 Prompt + 历史 → DeepSeek → SSE 流式输出            │
│    → 可选: extract_action_draft → 生成可执行草稿              │
└──────────────────────────────────────────────────────────────┘
```

---

## 十一、测试架构

```
测试结构:
                   ┌────────────────────────┐
                   │ 浏览器/全链路脚本与手工 │
                   │ 场景验证（非零覆盖声明）│
                   └────────────────────────┘
               ┌──────────────────┐
               │ 后端路由/集成测试 │
               │ 数据、RAG、状态机 │
               └──────────────────┘
          ┌─────────────────────────────┐
          │ 前端 Vitest 服务/组件/页面测试 │
          └─────────────────────────────┘
```

| 验证项 | 当前结论 | 备注 |
|------|------|------|
| 前端单元/组件测试 | `128 passed` | 最近已执行的完整前端测试 |
| 前端 lint / 生产构建 | 通过 | 当前前端工程验证 |
| 后端相关选择性测试 | `34 passed` | 已覆盖本轮变更关联链路 |
| 后端全量测试 | 未作为通过结论 | 曾受真实 LLM 外部调用与 Milvus 慢测影响而中止，需隔离外部依赖后单独验收 |

---

## 十二、环境变量全表

| 变量 | 用途 | 默认值 | 必填 | 影响范围 |
|------|------|------|:--:|------|
| `APP_ENV` | 运行环境 | dev | — | 生产环境保护、日志级别 |
| `AUTH_MODE` | 认证模式 | header | ✅ | 全局认证行为 |
| `AUTH_JWT_SECRET` | JWT 签名密钥 | — | jwt 模式必填 | JWT 签发验证 |
| `AUTH_ISSUER` | JWT 签发者 | — | jwt 模式 | JWT claims |
| `AUTH_AUDIENCE` | JWT 接收方 | — | jwt 模式 | JWT claims |
| `AUTH_JWKS_URL` | JWKS 公钥地址 | — | oidc 模式 | OIDC 验证 |
| `DEEPSEEK_API_KEY` | LLM API 密钥 | — | ✅ | 所有 Agent 节点 + 助手 |
| `STORAGE_BACKEND` | 数据库类型 | sqlite | — | 事务 + 状态存储 |
| `DATABASE_URL` | MySQL 连接串 | — | mysql 模式 | 事务数据库 |
| `MYSQL_HOST/PORT/USER/PASSWORD/DB` | MySQL 细节 | — | — | State Store |
| `DB_POOL_SIZE/DB_MAX_OVERFLOW/DB_POOL_RECYCLE` | 连接池 | 5/10/3600 | — | 数据库并发 |
| `MILVUS_HOST` | Milvus 地址 | localhost | ✅ | RAG 检索 |
| `MILVUS_PORT` | Milvus 端口 | 19530 | — | RAG 检索 |
| `RAG_MODEL` | 向量模型 | MiniLM | — | RAG 编码质量 |
| `RAG_IVF_NLIST` | IVF 聚类数 | 128 | — | RAG 检索速度 |
| `NEO4J_URI` | Neo4j 地址 | bolt://127.0.0.1:7687 | — | 证据图谱 |
| `NEO4J_USERNAME/PASSWORD/DB` | Neo4j 认证 | — | — | 证据图谱 |
| `REDIS_URL` | Redis 连接 | redis://localhost:6379/0 | — | 会话缓存 |
| `FHIR_ADAPTER_URL` | FHIR 适配器 | — | — | 外部 HIS 集成 |
| `SKIP_BRIDGE` | 跳过 Bridge | false | — | 开发调试 |
| `SKIP_EXTERNAL` | 跳过外部集成 | false | — | 开发调试 |
| `PUBLIC_BASE_URL` | 公共服务 URL | — | — | CDS Hooks |
| `LOG_LEVEL` | 日志级别 | INFO | — | observability |
| `CONTACT_ENCRYPTION_KEY` | 随访联系人加密密钥 | — | ✅ | 数据安全 |
| `MANAGEMENT_OPERATIONS_ENABLED` | 生产管理写开关 | false | production 写操作 | 需同时有管理员 claim |
| `GRAPH_MODE` | Agent 图模式 | classic | — | `stateful` 当前明确禁用 |
| `VITE_DEV_API_TARGET` | Vite 开发代理后端 | http://127.0.0.1:8001 | dev | 当前本地恢复联调；正式发布由部署配置统一指定 |
| `VITE_AUTH_MODE` | 前端认证模式 | header | — | 前端认证桥接 |
| `VITE_API_BASE` | 前端 API 基址 | (空=相对路径) | — | Vite proxy |

---

> 文档版本 v2.0 · 12 章 · 当前源码结构与运行架构 · 2026-07-21 · 环境变量详情见同目录《环境变量全表》
